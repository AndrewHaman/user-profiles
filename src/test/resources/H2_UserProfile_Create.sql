CREATE TABLE UserProfile (UserProfileId VARCHAR(64) NOT NULL PRIMARY KEY, Username VARCHAR(64) NOT NULL, Email VARCHAR(64) NULL, Phone VARCHAR(32) NULL, Firstname VARCHAR(64) NOT NULL, Lastname VARCHAR(64) NOT NULL, CreatedDate DATETIME NOT NULL, UpdatedDate DATETIME NOT NULL);